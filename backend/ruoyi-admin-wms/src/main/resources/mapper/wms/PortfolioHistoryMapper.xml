<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.wms.mapper.PortfolioHistoryMapper">

    <select id="getPortfolioHistoryList" resultType="com.ruoyi.wms.domain.entity.PortfolioHistory">
        SELECT cph.*
        FROM crypto_portfolio cp
        LEFT JOIN crypto_portfolio_history cph ON cph.portfolio_id = cp.id
        WHERE cp.id = #{portfolioId}

    </select>
</mapper>
